[project]
name = "supo-codegym"
version = "0.1.0"
description = "Replication of SUPO (arXiv 2510.06727) Table 1 CodeGym rows with Qwen3.5-9B on verl"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["supo*"]
